Title   : Virtual Machine Tap (VMtap)
Author  : Chung Hwan Kim <chunghwn@cs.utah.edu>

CONTENTS

1. Concepts: VMtap
2. Architectures Supported
3. Configuring VMtap
4. API Reference
5. VMtap Features and Limitations
6. Probe Overhead
7. VMtap Example
8. Utah A3 Project Team

1. Concepts: VMtap

2. Architectures Supported

3. Probe Point Specifications

kernel.function(PATTERN)
kernel.function(PATTERN).call kernel.function(PATTERN).return kernel.function(PATTERN).return.maxactive(VALUE) kernel.function(PATTERN).inline kernel.function(PATTERN).label(LPATTERN) module(MPATTERN).function(PATTERN) module(MPATTERN).function(PATTERN).call module(MPATTERN).function(PATTERN).return.maxactive(VALUE) module(MPATTERN).function(PATTERN).inline kernel.statement(PATTERN) kernel.statement(ADDRESS).absolute module(MPATTERN).statement(PATTERN)

4. API Reference

5. VMtap Features and Limitations

6. Probe Overhead

7. VMtap Example

8. Utah A3 Project Team

The following people have made major contributions to VMtap:
Chung Hwan Kim - chunghwn@cs.utah.edu
